package:
  name: sha3
  dependencies:
    - prelude
    - effect
    - console
    - integers
    - arrays
    - maybe
    - partial
    - foldable-traversable
    - tuples
    - strings
    - enums
    - wasm-base

workspace:
  packageSet:
    registry: 77.4.0
  backend:
      cmd: purs-wasm
      args: ["build", "-p", "node", "-E", "-e", "Main"]
  extraPackages:
    wasm-base:
      git: "https://github.com/harryprayiv/purescript-wasm-base.git"
      ref: "fa60442b1e53e882811026a7222db7adc3909da8"